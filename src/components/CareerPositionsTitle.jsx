import React from 'react';

const CareerPositionsTitle = (props) => {
  return (
    <div className={'career-positions-title'}>
      {props.children}
    </div>
  );
};

export default CareerPositionsTitle;
